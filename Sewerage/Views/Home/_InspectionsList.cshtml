﻿<div data-bind="if: $root.ChosenSectionId() != null">
     @*    <table class="observationTable">
        <tr>
            <thead>
                <th>Start date</th>
                <th>End date</th>
                <th>Video Url</th>    
            </thead>
            <tbody data-bind="foreach: Inspections">
                <tr data-bind="css: { selected: InspectionId == $parent.ChosenInspectionId() }, click: $parent.selectInspection">
                    <td data-bind="text: StartDate"></td>
                    <td data-bind="text: EndDate"></td>
                    <td data-bind="text: VideoUrl"></td>
                </tr>
            </tbody>
        </tr>
    </table>*@
     <ul class="nav nav-tabs" data-bind="foreach: Inspections">
         <li data-bind="css: {active: InspectionId == $parent.ChosenInspectionId() }, click: $parent.selectInspection">
             <a href="#">Inspection <span data-bind="text: InspectionId"></span></a>
         </li>
     </ul>
</div>
